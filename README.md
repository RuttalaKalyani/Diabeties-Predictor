# Diabeties-Predictor
A hybrid diabetes risk prediction system that converts raw patient text into structured data and analyzes risk using clinical rules (HbA1c, BMI, family history) combined with ML models (Logistic Regression/Random Forest). Features include automated text parsing, database storage, dashboards, and explainable, accurate predictions
